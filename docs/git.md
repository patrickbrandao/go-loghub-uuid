
# Git

```
git init;
git add .;

git commit -m "Initial commit: go-loghub-uuid implementation";

# Configurar autor
git config --global user.name patrickbrandao
git config --global user.email patrickbrandao@gmail.com


# Atualiza a URL do repositÃ³rio remoto "origin" com a URL correta e completa
git remote set-url origin https://github.com/patrickbrandao/go-loghub-ident.git

# Tenta enviar a branch local "main" para o remoto "origin"
git push -u origin main


# Cria a tag (o -f fo
